spring:
  redis:
    host: ${SPRING_REDIS_HOST:localhost}
    port: ${SPRING_REDIS_PORT:6379}
  data:
    mongodb:
      uri: mongodb://${SPRING_MONGO_HOST:localhost}:${SPRING_MONGO_PORT:27017}/${SPRING_MONGO_DB:my_db}
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_HOST:localhost}:${SPRING_KAFKA_PORT:9092}
  # -- wrong value--
  jwt:
    expiration:
      accessExpire: 86400000
      refreshExpire: 86400000
      guestExpire: 86400000
    secret: ${JWT_SECRET:0123456789ABCDEFGHIJ0123456789ABCDEFGHIJ}
clova:
  host: a
  api-key: b
  api-key-primary: c
lambda:
  endpoint: ${LAMBDA_ENDPOINT:localhost}
cloud:
  aws:
    credentials:
      access-key: e
      secret-key: f
    region:
      static: g
    s3:
      bucket: h
      base-url: i

logging:
  level:
    org.springframework.security: DEBUG
